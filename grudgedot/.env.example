# grudgedot — copy to .env, never commit .env
# ─────────────────────────────────────────────
# GitHub App (https://github.com/settings/apps)
GH_APP_ID=3101811
GH_CLIENT_ID=Iv23liLWxaTCRF5yqw1U
GH_SECRET=                    # client secret from App settings
GH_WEBHOOK_SECRET=            # set in App → Webhook → Secret

# Fallback PAT (read:org, read:packages) for unau